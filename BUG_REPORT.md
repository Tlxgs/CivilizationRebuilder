# 《文明重建者》代码审查报告（修订版）

审查范围：引擎层 14 个文件 + UI 层 20 个组件 + 配置层 9 个配置。
方法：逐文件通读 + `vm` 沙箱实跑验证。

> **本版是对上一版的更正。** 上一版有 4 条误报，已在下方「误报更正」中逐条说明原因。
> 本版只保留**能用真实操作路径复现**的结论；拿不准的一律标注为「待确认」，不写成确凿。

---

## 一、实测确认的问题

### ① 2 倍速按钮：可见可点但必然失败

**位置**：`ui/actions.js`（condition）vs `core.js:119`（引擎判据）

```js
// UI 显示条件
condition: () => GameState.speed === 2 || (GameState.resources['时间晶体']?.amount || 0) >= 0.1
// 引擎实际要求
if (ResourcesManager.getAmount("时间晶体") < 1) { alert("时间晶体不足！"); return false; }
```

**实测**：晶体数量落在 `[0.1, 1)` 区间时，按钮显示为可用，点击后弹「时间晶体不足！」并返回 false。

**测试构造**：直接比对两处源码常量（0.1 vs 1），无需模拟运行。
**差异说明**：无。这是纯常量不一致，读代码即可判定。

**影响**：玩家攒到 0.5 晶体时按钮亮着，点了没反应还弹窗。属明确的交互失效。

> 附带更正：上一版说「引擎根本不扣晶体」。**这是错的**——`gameloop.js:96-112` 的
> `startCrystalConsume()` 每 1000ms 执行 `crystal.amount -= 1`。2 倍速确实扣费。

---

### ② `softReset` 不清 `autoWarEnabled`

**位置**：`logic.js:1-69`

**实测**（走真实 `softReset(1,0,0,0,0)` 调用，不手改状态）：

```
软重置后 autoWarEnabled = true   ← 残留
```

**测试构造**：`initGameData()` → 设 `autoWarEnabled = true` → 调用真实 `softReset()` → 读回。
**差异说明**：无。就是真实函数调用。

**触发链与影响评估**：

`gameloop.js:50-57` 的自动开战判据是：
```
arms.amount >= arms.cap - 1e-4  &&  arms.cap > 100.001  &&  晶体库 < 6
```
- 重置后军备 `amount=0`，条件**当前不满足**，不会立刻开战。
- 但玩家重建 6 座以上「军营」（每座 `caps: {军备: 20}`）把军备攒满后，**会在没勾选的情况下自动开战**。

**结论**：这是真实的跨局状态污染，但触发需要玩家重建军事产能。**低危，但确实是 bug**，修法一行：`softReset` 开头补 `GameState.autoWarEnabled = false;`。

---

### ③ `softReset` 不清 `buildings[].mode`

**实测**：重置后 `裂变反应堆.mode = 1`（应为 0）。

**影响**：重置后建筑 `count=0`，不参与生产，**无即时影响**；但玩家重建该建筑后会沿用重置前的模式，与「重置回初始状态」的预期不符。**低危。**

---

### ④ 队列组件用 `:key="index"`

**位置**：`ui/queue.js:9`

`v-for="(item, index) in queue" :key="index"`。移除队列中间项时，后面的项会因 Vue 复用节点而短暂错位。

**影响**：一帧的视觉错位，不改变数据。**低危。**
**修法**：入队时给每项生成唯一 `uid`，用它当 key。

> 同上，`ui/crystal.js:36` 也用 index 作 key。但晶体对象自带稳定 `id`，改起来更容易。

---

### ⑤ `removeFromQueue` 越界无防护

**实测**：`removeFromQueue(99)` 抛 `TypeError: Cannot read properties of undefined (reading 'type')`。

**但**：`ui/queue.js:11` 的调用点是 `@click="remove(index)"`，`index` 来自 `v-for`，**不可能越界**。

**结论**：这是防御性缺失，不是可达 bug。**列在此仅作加固建议，不建议当 bug 修。**

---

## 二、已确认**不是** bug / 误报更正

| 上一版声称 | 实测更正 |
|---|---|
| **BUG-1 建筑效率归零后永久瘫痪** | **误报。** 完整链路（帐篷供人 → 伐木场用人）实测：效率 0 → 3.38e-5 → 7.59e-5 → 1.71e-4 → … → 第 40 次 compute 后**回到 1.0**。机制是自洽的指数回升。我上轮用同步快跑、且没提供人口，构造不等价于运行时。用户实测「会给资源过段时间确实回升」是准确的。 |
| **BUG-2 贸易量上限为 0 时返回 NaN** | **误报。** `getMaxTradeVolume`（`tradeEngine.js:6-15`）只累加建筑数量，**全项目没有任何减少建筑数的代码**（已全文件搜索确认无拆除功能）。操作路径不可达。 |
| **BUG-3 永久升级可致成本为负** | **误报。** 含 `costRatio` 的永久升级**只有 10 条**，满级 `getAdditiveValue('global.cost') = -0.41`，因子 `0.59`，距离 `-1` 尚远。我上轮推演的「20 级 / 25 级」是**配置里不存在的等级**，纯属虚构。 |
| **时间晶体不扣除** | **误报。** `gameloop.js:96-112` 明确 `crystal.amount -= 1`。用户实测「确实在扣除」正确。 |
| **`canAfford` 缺浮点 epsilon 会误判** | **未复现。** 实测 `0.1+0.2 >= 0.3` 返回 `true`，边界情形正常。加重 eps 是加固建议，不是 bug。 |

**唯一残留的真实顾虑**（非 bug，属代码质量问题）：

- `effectsManager.js:159` 把 `global.cost` 注册为**乘法**，而 `upgradesConfig.js:16` 用 `getAdditiveValue` **加法**读取。当前 10 级时两法结果 `0.59`（加法）vs `0.5688`（乘算），偏差小、无可见影响。但**语义不一致**，将来若再扩 `costRatio` 升级会放大偏差。**建议统一，但不紧急。**

---

## 三、修复优先级

**建议修（低风险、一行左右）**
1. `ui/actions.js` 2 倍速阈值 `0.1` → `1`，与引擎一致
2. `softReset` 补 `GameState.autoWarEnabled = false`
3. `softReset` 补 `bd.mode = 0`

**可选加固**
4. 队列 / 晶体列表的 `:key` 改用稳定 id
5. `global.cost` 加法/乘法语义统一 + 成本倍率兜底下限

---

## 四、关于本版的方法论

上一版之所以错得离谱，根因是**用推理代替验证**：

- 拿「假想的操作路径」当结论（如凭空的建筑拆除、虚构的升级等级）；
- 写了诊断脚本，却没警觉脚本构造与真实运行环境的差异（同步快跑 vs 200ms tick、没给人口）；
- 看到脚本输出的数字就当真，没回头核对前提。

本版所有结论都来自两类来源之一：**读源码常量比对**，或**用真实函数调用 + 真实配置跑出数字**。
凡涉及多步状态的推断（如自动开战触发链），都写明了「触发需要什么条件、当前是否满足」，不写成确定的危害。
